@using HRM.Infrastructure.Utilities;
@{
    Layout = null;
    var _hrm_Hre_Service = ConstantPathWeb.Hrm_Hre_Service;
    var _hrm_Main_Web = ConstantPathWeb.Hrm_Main_Web;
}
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ProcessApprovedPage</title>
    <script src="@Url.Content("~/Scripts/kendo/2014.1.415/jquery.min.js")"></script>
</head>
<body>
    <script>
    $(document).ready(function () {

        $.ajax({
            type: 'POST',
            url: '@_hrm_Hre_Service' + "Att_GetData/Set_ApproveRoster_ByModuleApprove",
            data: {
                host: '@_hrm_Main_Web',
                UserLoginID: '@ViewBag.Login',
                selectedIds: '@ViewBag.Record',
                },
                dataType: 'json',
                async: false,
                success: function (data) {
                    $('#statusText').html('Phê Duyệt Ca Làm Việc Thành Công');

                    @*if (data.Status == '@DataErrorCode.Success.ToString()') {
                    $('#statusText').html('Phê Duyệt Ca Làm Việc Thành Công');
                }
                else if (data.Status == '@DataErrorCode.Success_Forward.ToString()') {
                    $('#statusText').html('Phê Duyệt Ca Làm Việc Và Chuyển Tiếp Thành Công');
                }
                else if (data.Status == '@DataErrorCode.Error_NoUserApproved.ToString()') {
                    $('#statusText').html('Bạn Chưa Chọn Người Duyệt Kế Tiếp Cho Ca Làm Việc');
                }
                else if (data.Status == '@DataErrorCode.Error_Reject.ToString()') {
                    $('#statusText').html('Ca Làm Việc Đã Từ Chối Không Được Phép Phê Duyệt');
                }
                else if (data.Status == '@DataErrorCode.NoPermission.ToString()') {
                    $('#statusText').html('Tài Khoản Không Có Quyền Duyệt Ca Làm Việc Này');
                }
                else if (data.Status == '@DataErrorCode.Locked.ToString()') {
                    $('#statusText').html('Đối Tượng Này Đã Bị Khóa');
                }
                else {
                    $('#statusText').html('Duyệt Đối Tượng Thất Bại');
                }*@
            }
        });

    });

</script>

    <div style="text-align:center">
        <h1>Trang Xử Lý Duyệt!!!</h1>
        <span id="statusText"></span>
    </div>
</body>
</html>
