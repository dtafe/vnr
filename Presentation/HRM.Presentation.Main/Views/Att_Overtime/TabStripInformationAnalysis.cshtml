@using HRM.Presentation.UI.Controls.Kendo.TabStrip

@using HRM.Infrastructure.Utilities;
@{
    var tab = new TabStripInfomation
    {
        Name = "tabStripOvertime",
    };

    var t1 = new ItemsInformation
    {
        Content = PartialExtensions.Partial(@Html, "CreateOrUpdateAnalysis", Model).ToHtmlString(),
        Selected = true
    };


    tab.TabItems.Add(ConstantDisplay.HRM_Attendance_Overtime_Info.TranslateString(), t1);

}
@(Html.TabStripControl(tab))
